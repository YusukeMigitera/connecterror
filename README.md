# connecterror

`connecterror` validates proper use of Connect RPC error codes.

## Install

```sh
$ go get github.com/YusukeMigitera/connecterror/cmd/connecterror
```

## Usage

```sh
$ go vet -vettool=`which connecterror` pkgname
```
